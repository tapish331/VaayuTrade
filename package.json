{
  "name": "vaayutrade-repo-tooling",
  "private": true,
  "version": "0.0.0",
  "description": "Repo-wide tooling for VaayuTrade (conventional commits + husky hooks).",
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD",
    "commitlint:edit": "commitlint --edit",
    "postinstall": "node -e \"console.log('Tooling installed. Run: npx husky install')\""
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
